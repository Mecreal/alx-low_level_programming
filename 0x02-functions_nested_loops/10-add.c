#include "main.h"

/**
 * add -calculate the additions value
 * @a: a number to be checked
 * @b: seconde number
 * Return: 1 - if lowercase 0 id otherwise
*/

int add(int a, int b)
{
	return (a + b);
}

#include "main.h"

/**
* _strlen - function calculating the length of a string
* @s : the pointer of the string
* Return: lenght
*/


int _strlen(char *s)
{
	int c = 0;

	while (*s != '\0')
	{
		i++;
		s++;
	}
	return (c);
}
